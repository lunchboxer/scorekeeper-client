import React from 'react'
import Typography from 'material-ui/Typography/Typography'

const ClassSessionEnded = ({ session }) => {
  return (
    <div>
      <Typography type="subheading">Ended</Typography>
    </div>
  )
}

export default ClassSessionEnded
